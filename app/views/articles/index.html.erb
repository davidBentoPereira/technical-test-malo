<p style="color: green"><%= notice %></p>

<h1>Articles</h1>

<%= search_form_for @q do |f| %>
  <%= f.label :title_or_content_or_tags_cont %>
  <%= f.search_field :title_or_content_or_tags_cont %>

  <%#= f.label :title_or_content_or_tags %>
  <%#= f.search_field :title_or_content_or_tags %>

  <%= f.submit %>
<% end %>

<div id="articles">
  <% @articles.each do |article| %>
    <%= render article %>
    <p>
      <%= link_to "Show this article", article %>
    </p>
  <% end %>
</div>

<%= link_to "New article", new_article_path %>
